# Verilog_CPU
Custom CPU design in Verilog with pipelined datapath (fetch, decode, execute, write-back), ALU, register file, and FSM-based control unit. Includes hazard handling, ROM/RAM integration, and testbench verification.
